# Assets

No demo image, dataset, model weight, ONNX model, or TensorRT engine is bundled.

Pass your own image to the scripts. Official `yolo11s.pt` weights may be downloaded by
Ultralytics on first use and are ignored by Git.

